#include <stdio.h>

/**
 * main - Entry point
 * This is a program that prints a new line using printf function
 * Return: Always 0 and shows sucess
 */
int main(void)

{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
