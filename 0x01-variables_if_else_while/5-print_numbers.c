#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 * prints all single digit numbers of base 10 starting from 0
 * Return: value is 0
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	printf("%d", i);
	printf("\n");

	return (0);
}
