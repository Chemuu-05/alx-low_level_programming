#include "main.h"

/**
 * mul - multiplies two integers
 * @a: The first number to multiply
 * @b: The second number to multiply the first
 * Return: The result of multiplication
 */

int mul(int a, int b)
{
	return (a * b);
}
